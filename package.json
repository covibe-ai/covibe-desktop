{
  "name": "covibe-desktop",
  "private": true,
  "scripts": {
    "dev": "bun run build:dev && electrobun dev",
    "build:dev": "bun install && electrobun build",
    "build:prod": "electrobun build --release"
  },
  "dependencies": {
    "electrobun": "^1.18.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
